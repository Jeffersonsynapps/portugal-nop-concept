# Status — Gastengids Carvoeiro

**Datum:** 4 juli 2026
**Versie:** live op GitHub Pages — https://jeffersonsynapps.github.io/portugal-nop-concept/

## Wat klaar is

- `dashboard.html` aangemaakt
- Content uit klant-WhatsApp volledig geïnventariseerd:
  - Supermarkten, bakker
  - Restaurants
  - Borrel
  - 3 afstandscategorieën te-doen (bezienswaardigheden)
  - Shoppen
  - Excursies
  - Vervoer
  - Inspiratie
- Preview A gekozen als basisrichting (effen achtergrond, 2-koloms grid)
- Hero-overlay lichter gemaakt zodat aquarelkleuren goed zichtbaar blijven
- Hero-tekst vereenvoudigd naar "Welkom" (plaatsnaam verwijderd)
- Grid-layout omgezet: categorieën als kaarten in 2-koloms grid, responsive naar 1 kolom op smal scherm
- Inspiratie als eigen categorie-box afgesplitst
- Grid herstructureerd: 9 losse categorie-boxen samengevoegd tot 4 overkoepelende boxen (Boodschappen & shoppen, Eten & drinken, Te doen in Carvoeiro, Vervoer) met subkopjes en scheidingslijnen per subgroep
- Layout omgezet naar 1-kolom rubrieken (volle breedte) met auto-fit subgroepen naast elkaar binnen elke rubriek
- Hero-afbeelding vervangen door video-playlist systeem: `assets/hero-videos/video-1.mp4`, autoplay muted playsinline, playbackRate 0.2×, object-fit cover; fade-out in laatste 0,5 seconde + fade-in bij volgende video via CSS opacity transition; playlist-array in JS maakt later toevoegen van video-2.mp4 e.v. triviaal
- Hero-overlay en witte overlay-tekst verwijderd; welkomsttekst nu direct onder de video in teal-dark kleur
- Wisselende tekst-overlay toegevoegd op de video (Welcome / Enjoy / Relax / Take your time); elke 4,5 seconden fade-out → nieuw woord → fade-in op willekeurige positie; positie berekend op basis van actuele elementbreedte vs herohoogte/breedte zodat tekst nooit buiten beeld valt op enige schermgrootte
- Hero-mediacyclus uitgebreid met foto-fase: Fase 1 (video-playlist eenmaal door) → Fase 2 (5 foto's × 10 sec + 1,8s fade, uit assets/hero-photos/foto-1..5) → terug naar Fase 1; tekst-overlay loopt volledig onafhankelijk door beide fases heen
- Modal/bottom-sheet patroon gebouwd als herbruikbaar systeem (modalData object + openModal/closeModal functies); proef: Padaria Fabrica Velha (bakker-kaart) — desktop gecentreerd met overlay, mobiel bottom-sheet; inhoud: foto-placeholders, titel, beschrijving, Google Maps embed + "Open in Google Maps" link; uitbreiden naar andere kaarten = 1 object toevoegen aan modalData + click-handler
- Nieuw tabblad "Locatiegegevens" toegevoegd aan `dashboard.html`, naast Takenlijst, Open vragen & besluiten en Planning: 3 tabellen (Boodschappen & shoppen, Eten & drinken, Te doen) met kolommen Naam/Adres/Website-Social/Opmerking/Gedaan (aanvinkbare checkbox, status bewaard via localStorage)
- Alle adressen/websites in deze tabellen zijn geverifieerd via websearch en exact vastgelegd zoals aangeleverd door klant; typefout gecorrigeerd (Mar de Flora → Mar de Fora)
- Modal-patroon (`preview-a.html`) aangepast op basis van test met de bakker-kaart: dubbele "Open in Google Maps"-link verwijderd (alleen de blauwe knop in de kaart-embed zelf blijft), onderaan de modal komt in plaats daarvan een website-link (alleen getoond als er een website in de data staat), foto-placeholders ("Foto volgt") tonen niet meer standaard — foto-sectie wordt pas getoond zodra een item een gevulde `photos[]`-array heeft
- Alle items onder "Boodschappen & shoppen" zijn nu klikbare kaarten met hetzelfde modal-patroon: Apolonia, Aldi, Intermarché, Twee kleine winkeltjes, Mais Perto, Padaria Fabrica Velha, MAR Shopping centre / Designer Outlet, Forum Algarve — elk met Google Maps-embed op basis van naam+adres; website-links toegevoegd waar aangeleverd, plus zelf gevonden officiële sites voor MAR Shopping (marshopping.com/algarve) en Forum Algarve (forumalgarve.net)
- Alle 8 modals getest via headless Chromium (CDP): overlay opent, titel/beschrijving/kaart-embed correct, foto-sectie verborgen waar geen foto's, website-link alleen zichtbaar waar aanwezig, geen dubbele maps-link meer
- Adres toegevoegd voor "Twee kleine winkeltjes": Rua dos Pescadores, de hoofdstraat van Carvoeiro (bevestigd via websearch, geen specifiek huisnummer bekend) — bijgewerkt in `dashboard.html` en gebruikt voor de Google Maps-embed van dit item in `preview-a.html`
- Technische fix: een straatnaam zonder huisnummer levert geen pin op in de Google Maps embed. In `preview-a.html` is voor "Twee kleine winkeltjes" een apart `mapsAddress`-veld toegevoegd ("Rua dos Pescadores 35, Carvoeiro", een bevestigd bestaand adres midden op deze straat) dat alléén als technisch ankerpunt voor de kaart-embed dient; de zichtbare tekst blijft de algemene "Rua dos Pescadores, Carvoeiro". Geverifieerd via screenshot van de modal in headless Chromium: pin verschijnt nu correct midden in Carvoeiro
- Zelfde `mapsAddress`-fix ook toegepast op "Mais Perto" (ankerpunt: "Monte Dourado, Carvoeiro", zichtbare tekst blijft "Bij Monte Dourado, Carvoeiro") — geverifieerd via screenshot: duidelijke pin op Monte Dourado
- "MAR Shopping centre / Designer Outlet" bleef ook met adres-ankerpunt zonder duidelijke pin: Google herkent de naam als bestaande zaak en toont een klein categorie-icoontje i.p.v. een pin. Eerste poging (pure coördinaten als query) gaf wel een pin, maar liet de "Openen in Maps"-knop verdwijnen. Uiteindelijke fix: `mapsQuery`-veld dat voor dit item alléén het adres gebruikt ("Avenida do Algarve, 8135-182 Almancil"), zonder de itemnaam ervoor — dat geeft zowel een losse rode pin als de "Openen in Maps"-knop terug. Zichtbare tekst blijft ongewijzigd "Loulé". Geverifieerd via screenshot
- Padaria Fabrica Velha (bakker) modal bijgewerkt: 2 echte foto's van de klant toegevoegd (`assets/locatie-fotos/padaria/bakker1.jpg` en `bakker2.jpg`, aangeleverd via Downloads), foto-sectie toont deze nu automatisch volgens de eerder gebouwde photos[]-logica. Website-link (pastelariafabricavelha.pt) verwijderd omdat deze niet werkt — de website-regel onderaan de modal verdwijnt daardoor vanzelf. Kaart-embed ongewijzigd. Geverifieerd via screenshot en DOM-check in headless Chromium
- Twee visuele fixes aan modal-patroon (geldt voor alle items): (1) de sluitknop (`.modal-header`) is losgemaakt van de document-flow (position:absolute, zwevend rechtsboven i.p.v. eigen rij) — dit was de daadwerkelijke bron van de lege ruimte bovenaan bij items zonder foto's; titel begint nu overal exact op dezelfde hoogte (20px) wanneer er geen foto's zijn, geverifieerd voor alle 7 items zonder foto. (2) Adres als leesbare tekstregel toegevoegd direct boven de Maps-embed, onder de beschrijving, met pin-icoontje en rustige kleinere typografie (geen link) — gebruikt hetzelfde `address`-veld dat al in de data stond, dus verschijnt automatisch bij alle 8 items. Getest op items met en zonder foto's (bakker vs. Aldi) en met lange titel (MAR Shopping, geen overlap met sluitknop)
- Volgorde van modal-elementen herschikt (geldt voor alle items met foto's): titel → beschrijving → foto's (indien aanwezig) → adres → Maps-embed → website-link (indien aanwezig). Foto-sectie (`.modal-photos`) is verplaatst van vóór `.modal-body` naar ertussenin (na de beschrijving, vóór het adres); padding aangepast naar margin-bottom omdat de sectie nu binnen de padding van `.modal-body` valt in plaats van ervoor. Getest op bakker (met foto's: nette volgorde titel/beschrijving/foto's/adres/kaart) en Aldi (zonder foto's: sectie slaat over zonder lege ruimte, adres direct na beschrijving, website-link onderaan blijft werken)
- Hero-video/foto's op mobiel werden horizontaal afgesneden door object-fit: cover (alleen middelste deel van de breedte zichtbaar). Eerste poging: horizontale pan-animatie (volledige hoogte + translateX-animatie gesynchroniseerd met video/foto-duur) — werkte technisch correct (geverifieerd met headless Chromium: pan liep exact synchroon met de echte videoduur van ~25s bij de huidige 0.4× playbackrate, en met de 10s foto-weergave), maar is op verzoek van de klant teruggedraaid vóór oplevering
- Nieuwe, gekozen aanpak: letterbox i.p.v. pan. Op mobiel (max-width: 600px, zelfde breekpunt) krijgen `#hero-video`/`#hero-photo` `object-fit: contain` in plaats van `cover`, met `background: var(--cream)` zodat de ontstane lege ruimte boven/onder het beeld bewust oogt i.p.v. een fout. De hero-hoogte blijft vast op 70vh (ongewijzigd), dus geen springerig gedrag tussen video/foto's met net andere beeldverhoudingen. Tekst-overlay-positionering (`placeOverlayRandom`) is aangepast: nieuwe helper `getVisibleMediaRect()` berekent op mobiel het daadwerkelijk zichtbare beeld-vlak (op basis van de natuurlijke afmetingen van de actieve video/foto en de contain-schaling), zodat willekeurige tekstposities altijd binnen dat vlak vallen i.p.v. in de crème letterbox-balken; op desktop blijft dit gewoon de volledige hero-box (geen gedragsverandering). Desktop ongewijzigd: object-fit blijft cover, geen letterbox. Getest met headless Chromium op 380×800 (video: contain bevestigd, cream-achtergrond `rgb(253,248,240)` bevestigd, overlay-posities binnen het zichtbare vlak over meerdere cycli; foto: zelfde hero-hoogte 560px, geen sprong) en op 1280×800 desktop (cover, volledige breedte, geen letterbox, ongewijzigd)
- Twee vervolgfixes op de letterbox-weergave: (1) `object-position: top` toegevoegd aan de mobiele media query, zodat het beeld bovenaan de hero-container begint i.p.v. verticaal gecentreerd — de crème letterbox-ruimte blijft nu alleen onderaan over. (2) `getVisibleMediaRect()` aangepast: `top` is nu altijd 0 op mobiel (in plaats van `(heroH - renderedH) / 2`), zodat de "veilige zone" voor de tekst-overlay meebeweegt met de nieuwe boven-uitlijning en nooit in de crème-ruimte onderaan uitkomt. Getest met headless Chromium op 380×800: video en foto beginnen beide exact op 0px vanaf de top van de hero (geen gat erboven), en 30+40 steekproeven van `placeOverlayRandom()` (video- én foto-fase) vielen allemaal binnen het zichtbare beeldvlak, geen enkele in de letterbox-strook
- Resterende crème-ruimte onderaan op mobiel volledig weggewerkt: `.hero`-hoogte is niet langer vast (70vh) op mobiel, maar volgt nu dynamisch de werkelijke beeldverhouding van de actieve video/foto (volledige breedte, hoogte "auto"). Nieuwe JS-functie `updateHeroHeight()` berekent bij elke `loadedmetadata` (video) resp. `load` (foto) de hoogte als `heroBreedte × (natuurlijkeHoogte / natuurlijkeBreedte)` en zet die als expliciete pixelhoogte; CSS `transition: height 0.3s ease` (mobiel-only) voorkomt schokkerig gedrag wanneer video en foto's een net iets andere beeldverhouding hebben. `getVisibleMediaRect()` is vereenvoudigd tot gewoon de volledige hero-box (mobiel én desktop), omdat er nu nergens meer letterbox-ruimte overblijft.
  - **Bug gevonden en gefixt tijdens het testen:** de CSS `aspect-ratio: 16/9`-fallback op `.hero` (bedoeld om inklappen naar 0 hoogte te voorkomen vóór de JS-berekening) zorgde ervoor dat de browser i.p.v. de hoogte juist de *breedte* herberekende zodra JS een vaste hoogte instelde (met `width` nog op `auto`) — dit veroorzaakte een cascaderende fout waarbij de container bij foto-3 (afwijkende beeldverhouding, 4080×3072) op 509px breed werd berekend i.p.v. 380px. Fix: `width: 100%` expliciet toegevoegd aan `.hero` in de mobiele media query, wat de aspect-ratio-berekening neutraliseert. Na de fix kloppen alle 5 foto's + de video exact met de verwachte hoogte (breedte × beeldverhouding), geverifieerd per item met een geïsoleerde test
  - Getest op 380×800: video (213.75px, geen gat), alle 5 foto's stuk voor stuk met wisselende hoogtes (213.75–286.11px afhankelijk van elk beeldformaat, top/bottom-gap altijd 0), 30 overlay-steekproeven binnen bounds, desktop 1280×800 ongewijzigd (vaste 560px hoogte, cover, volledig gevuld)
- `dashboard.html` tab "Locatiegegevens" (tabel Boodschappen & shoppen) opnieuw gesynchroniseerd met de adres-/website-correcties die inmiddels in `preview-a.html` zijn doorgevoerd: Mais Perto-adres gelijkgetrokken ("Bij Monte Dourado, Carvoeiro"), Padaria Fabrica Velha website leeggemaakt (pastelariafabricavelha.pt werkt niet, verwijderd uit live site), MAR Shopping en Forum Algarve websites ingevuld (marshopping.com/algarve, forumalgarve.net — dezelfde waarden als in preview-a.html). Alle 8 items in deze tabel hebben inmiddels een werkende Google Maps-embed in preview-a.html en staan daarom standaard op "Gedaan" aangevinkt (nieuw `done`-veld in de item-data als standaardwaarde; bestaande handmatige aan/uit-toggles via localStorage blijven leidend als een gebruiker het zelf ooit aanpast). Eten & drinken en Te doen zijn nog niet gesynchroniseerd omdat preview-a.html voor die twee rubrieken nog geen klikbare kaarten/modal-data heeft
- Typografische hiërarchie op de contentpagina hersteld: de intro-tekst onder de hero ("Welkom, jouw persoonlijke gids...") was te licht/klein voor een welkomstboodschap, terwijl de rubriek-titels (Boodschappen & shoppen, Eten & drinken, Te doen in Carvoeiro, Vervoer) zwaarder oogden dan gewenst voor navigatie-elementen. `.hero-caption` aangepast van font-weight 300/font-size ~17-22px naar font-weight 500/font-size ~21-30px (max beschikbaar Fraunces-gewicht); `.category-header h2` verlaagd van font-weight 400/20px naar font-weight 300/17px. Geverifieerd via screenshots op desktop (1280px) en mobiel (390px): intro-tekst is nu duidelijk het zwaarste element na de hero, rubriek-titels ogen rustig/ondergeschikt
- Wisselende tekst-overlay (Welcome/Enjoy/Relax/Take your time) op mobiel (≤600px) verkleind naar ~60% van de desktop-grootte via `calc(clamp(33px, 5.25vw, 63px) * 0.6)`; desktop ongewijzigd. Eerste versie van deze media-query stond per ongeluk vóór de basis `#hero-overlay`-regel in de stylesheet, waardoor de latere basisregel'm bij gelijke specificiteit overschreef (bug gevonden tijdens testen, opgelost door de mobiele override ná de basisregel te plaatsen). De vaste marge in `placeOverlayRandom()` is ook verkleind op mobiel (20px → 12px), consistent met de kleinere tekst. Geverifieerd op 390×800: font-size 19.8px (60% van 33px) tegenover 63px op desktop, en 40 steekproeven van willekeurige tekstposities vielen allemaal binnen de veilige zone (geen enkele afgesneden aan de randen)
- Project gepusht naar GitHub (`jeffersonsynapps/portugal-nop-concept`, branch `main`) en live via GitHub Pages. `index.html` (de root-URL die Pages standaard serveert) liep achter op `preview-a.html` — overschreven zodat de live site alle recente aanpassingen toont, geverifieerd byte-voor-byte identiek
- De 3 hoofdrubrieken (Boodschappen & shoppen, Eten & drinken, Te doen in Carvoeiro) zijn nu een accordion: klikbare header met chevron-icoon (draait 90° bij openen), standaard allemaal ingeklapt bij laden, onafhankelijk van elkaar open/dicht te klappen, vloeiende height-transitie via de CSS grid-template-rows 0fr/1fr-techniek (geen JS-hoogtemeting nodig). Subgroepen (Supermarkten, Bakker, etc.) hebben zelf geen eigen uitklap-gedrag — die tonen gewoon volledig zodra hun rubriek open is. Vervoer-box blijft ongewijzigd (geen accordion, altijd zichtbaar). Toegepast op zowel `preview-a.html` als `index.html`. Getest met CDP-aansturing in een zichtbaar (non-headless) venster op desktop (1280px) en mobiel (390px): alle 3 starten dicht, onafhankelijk te openen/sluiten, en een modal-item binnen een opengeklapte rubriek (Aldi) opent nog gewoon zonder conflict met de accordion-klik
- **`preview-a.html` is uitgefaseerd en verwijderd uit het project.** We werken vanaf nu niet meer met een aparte preview-kopie naast `index.html` — `index.html` is het enige, definitieve bestand voor de site. Vóór verwijdering geverifieerd dat beide bestanden byte-voor-byte identiek waren (op de `<title>`-tag na), dus `index.html` bevatte al de volledige, actuele versie inclusief de accordion. Verwijzingen naar `preview-a.html` verderop in dit logboek zijn historische aantekeningen over werk dat destijds in dat bestand is gedaan (nu allemaal onderdeel van `index.html`) en blijven staan als context, niet als verwijzing naar een nog bestaand bestand
- **Project herstructureerd naar 2 pagina's met gedeelde stijl.** Alle CSS is uit `index.html` gehaald naar een los bestand `styles.css`; de hero-mediacyclus (video-/foto-playlist, mobiele letterbox-logica) en de wisselende tekst-overlay zijn uit `index.html` gehaald naar een los bestand `hero.js`. Beide bestanden worden door beide pagina's ingeladen via `<link>`/`<script>`, zodat er geen dubbele code meer rondslingert.
  - De volledige, bestaande gastengids (Boodschappen & shoppen, Eten & drinken, Te doen in Carvoeiro, Vervoer, accordion, alle 8 modals) is hernoemd naar `in-de-buurt.html`. Functioneel ongewijzigd; gebruikt nu `styles.css`/`hero.js`. Footer heeft een nieuwe link "← Terug naar hoofdpagina" naar `index.html`.
  - `index.html` is nu een nieuwe hoofdpagina-in-opbouw (skelet, structuur alleen): dezelfde hero, een introsectie "Over dit huisje" (placeholder-tekst), een fotogalerij-sectie (placeholder-vakken, nog geen echte foto's), een CTA-knop "Boek dit huisje" (placeholder-link `TODO-BOEKINGSLINK`) en een link naar `in-de-buurt.html`. Inhoud van de hoofdpagina volgt later.
  - Beide pagina's getest in zichtbaar/gerenderd browservenster op desktop- en mobielformaat: geen overflow, hero-mediacyclus en tekst-overlay werken op beide pagina's identiek, accordion en modals in `in-de-buurt.html` functioneren ongewijzigd.
  - **Let op — QR-code:** de QR-code die al naar de klant is verstuurd wijst naar de root-URL van de site. Die root-URL toont vanaf nu de nieuwe (nog lege) hoofdpagina in plaats van de gastengids. Zodra deze herstructurering live staat, moet er een nieuwe QR-code naar de klant, óf moet de hoofdpagina eerst inhoudelijk af zijn vóór het live gaat.
- Echte boekingslink ingevuld op `index.html`: de "Boek dit huisje"-knop wijst nu naar `https://www.carvoeiroclubegroup.com/monte-dourado/`, opent in een nieuw tabblad (`target="_blank" rel="noopener"`). Placeholder `TODO-BOEKINGSLINK` is weg.
- Plattegrond van het resort toegevoegd op beide pagina's, als popup i.p.v. inline afbeelding. Aangeleverd bestand (`plattegrond resort.jpg` uit Downloads) gekopieerd naar `assets/plattegrond-resort.jpg` — bewust als `.jpg` behouden (het aangeleverde bestand was al een JPEG, geen PNG). Herbruikbare lightbox-component gebouwd (CSS in `styles.css`, gedrag in `hero.js` via `[data-lightbox-trigger]`/`.lightbox-overlay`), visueel in lijn met het bestaande locatie-modal-patroon (overlay, kruisje, fade/scale-animatie) maar zonder titel/beschrijving/kaart-embed — puur de afbeelding. Op mobiel (≤640px) krijgt de popup minder padding en een ruimere breedte (96vw) zodat de tekst op de plattegrond leesbaar blijft; native pinch-zoom blijft daarnaast gewoon beschikbaar omdat de viewport-meta geen `user-scalable=no` heeft.
- Vaste header met "Huisje Carvoeiro"-terug-link (eerder toegevoegd) weer verwijderd op verzoek — vervangen door expliciete knoppen. Op `index.html` staat naast "Boek dit huisje" nu een knoppengroep (`.cta-group`) met secundaire knoppen "Plattegrond Resort" (opent de popup) en "In de buurt" (naar `in-de-buurt.html`); de losse tekstlink "Bekijk de gastengids: In de buurt →" is daarmee vervallen. Op `in-de-buurt.html` staat onder het Vervoer-blok een knoppenrij (`.secondary-actions`) met "Plattegrond Resort" (zelfde popup, hergebruikt component) en "Home" (naar `index.html`); de bestaande footer-link "← Terug naar hoofdpagina" blijft ook staan.
- Alles hierboven getest via headless Chromium (CDP, node-script): booking-link/target/rel correct, plattegrond-popup opent/sluit correct op beide pagina's op desktop (1280×900) én mobiel (390×844), afbeelding leesbaar (natuurlijke grootte 595×431, geschaald), navigatieknoppen (In de buurt / Home) linken naar de juiste pagina's, `body.modal-active` wordt na sluiten weer correct verwijderd.
- Melding van klant dat de oude versie (grote inline plattegrond, tekstlink-knoppen) nog zichtbaar was, bleek bij nader onderzoek een browsercache-kwestie te zijn — bevestigd via: (1) slechts één server actief (poort 8765, working directory exact de projectmap, geen scratch-kopie), (2) curl-respons byte-voor-byte identiek (md5) aan de bestanden op schijf, (3) klant heeft het zelf bevestigd via een incognito-venster. Geen codewijziging nodig; `.cta-button-secondary`-stijl (lichte outline, gekleurde tekst, pil-vorm) is hiermee bevestigd als de definitieve secundaire-knopstijl voor de hele site.
- Knoppenvolgorde aangepast op verzoek: op `index.html` nu "Boek dit huisje" → "In de buurt" → "Plattegrond Resort" (laatste twee omgewisseld). Op `in-de-buurt.html` nu "Home" → "Plattegrond Resort" (omgewisseld).
- "Boek dit huisje" op `index.html` qua hoogte/padding gelijkgetrokken aan de secundaire knoppen: padding van `16px 40px`/font-size 17px teruggebracht naar `12px 24px`/font-size 14px (zelfde waarden als `.cta-button-secondary`), met behoud van het kleurverschil (terracotta vulling vs. teal outline). Alleen op `index.html` van toepassing; de knoppen op `in-de-buurt.html` waren al onderling consistent en zijn ongewijzigd.
  - **Bug gevonden en gefixt tijdens het testen:** de `<button>`-variant ("Plattegrond Resort", op beide pagina's) bleek native browser-knopstyling (`appearance: auto`) te behouden ondanks de custom CSS, waardoor `line-height` genegeerd werd en de knop ~5px korter uitviel dan de `<a>`-knoppen ernaast — onzichtbaar in eerdere screenshots omdat die altijd via een popup-klik ontstonden, niet in rusttoestand. Fix: `appearance: none` (+ `-webkit-appearance: none`) en een expliciete `line-height: 1.6` toegevoegd aan `.cta-button-secondary` (en voor consistentie ook aan `.cta-button`). Geverifieerd via CDP met correcte cache-uitschakeling (eerdere testronde in dit gesprek gaf zelf onterecht een verschil aan door een cachebug in het testscript, niet in de site): alle drie knoppen op `index.html` en beide op `in-de-buurt.html` nu exact 48.39px hoog met identieke `12px 24px`-padding, op desktop (1280×900) én mobiel (390×844).
- **Fotocarrousel + gallery + lightbox gebouwd op `index.html`**, ter vervanging van de 6 "Foto volgt"-placeholders in de "Over dit huisje"-sectie:
  - Bronmateriaal: 22 foto's uit `/home/jefferson/Downloads/wetransfer_fc1072vc-monte-dourado-26_2026-06-30_0856` (5 dronebeelden `DJI_0654–0667` + 17 camerafoto's `P1010005–P1010084`, origineel 4032×3024 t/m 4608×3456px, 5–10MB per stuk). Verkleind naar max. 1600px langste zijde / JPEG-kwaliteit 80 (met behoud van EXIF-rotatie) en hernoemd naar `assets/huisje-fotos/huisje-1.jpg` t/m `huisje-22.jpg` in oorspronkelijke bestandsvolgorde — resultaat ~4,9MB totaal i.p.v. ~140MB, noodzakelijk voor een site die primair via QR-code op mobiel wordt bekeken.
  - Carrousel toont 3 foto's naast elkaar (desktop), 2 (tablet ≤900px), 1 met peek (mobiel ≤640px), gebouwd op native `overflow-x:auto` + `scroll-snap-type:x` i.p.v. een zelfgeschreven sleep-handler — dat geeft swipe-ondersteuning op mobiel automatisch mee via de browser zelf, zonder extra JS (en zonder risico op een `preventDefault()` die swipe blokkeert). Pijl-knoppen roepen `track.scrollBy` aan. Autoplay elke 3,5s, pauzeert bij hover of bij handmatige interactie (pijl-klik of touch-start) en hervat na 4s inactiviteit — geverifieerd via CDP dat scrollpositie stapsgewijs opschuift, blijft staan tijdens hover, en na de pauzeperiode vanzelf hervat.
  - "Gallery"-knop rechtsboven de carrousel opent een overlay met alle 22 foto's in een grid (met introtekst, voor nu placeholder — definitieve tekst volgt later), zelfde overlay/modal-patroon als de plattegrond-popup, dus de hero blijft gewoon actief op de achtergrond.
  - Bestaande lightbox-component (gebouwd voor de plattegrond-popup) generiek gemaakt: `initLightbox()` geeft nu een `open(images, startIndex)`-API met prev/next-navigatie zodra de overlay-markup navigatie-knoppen bevat; ontbreken die (zoals bij de plattegrond-popup), dan werkt hij nog steeds als simpele 1-afbeelding-popup zoals voorheen — geen breaking change. Nieuwe gedeelde `#photo-lightbox`-overlay met pijlen wordt gebruikt vanuit zowel de carrousel-foto's als de gallery-grid-foto's, met wrap-around (laatste → eerste foto en omgekeerd).
  - Geverifieerd via headless Chromium (CDP): carrousel/autoplay/pauze/hervatten, "Gallery"-knop opent grid met 22 items, klik op grid-foto opent lightbox op de juiste foto, Next/Prev-navigatie werkt, plattegrond-popup nog steeds ongewijzigd werkend (geen navigatiepijlen zichtbaar, blijft 1 afbeelding), en expliciet bevestigd dat de hero (video) actief/`paused:false` blijft tijdens grid- en lightbox-gebruik — op zowel desktop (1280×900) als mobiel (390×844).
  - **Swipe op mobiel:** kon niet mechanisch bevestigd worden in deze headless testomgeving — drie verschillende technieken (losse touch-events, `Input.synthesizeScrollGesture`, wheel-event) kwamen niet eens aan bij de pagina (zelfs een simpele `touchstart`-listener triggerde niet), wat wijst op een beperking van touch-simulatie in headless Chromium, niet op een sitebug. Er zit geen `touch-action:none` of `preventDefault()` in de weg. Klant test dit later zelf op een echt mobiel toestel.
  - Bouw is bewust generiek opgezet (`initPhotoStrip()`) zodat de tweede, identieke fotostrip ("omgeving") later simpelweg een tweede aanroep is met een andere container/foto-set — inmiddels gebouwd, zie volgende punt.
- **Tweede fotostrip "Foto's van de omgeving" toegevoegd op `index.html`**, direct onder de knoppengroep (Boek dit huisje / In de buurt / Plattegrond Resort) en vóór de footer — exact dezelfde `initPhotoStrip()`-component hergebruikt, geen nieuwe CSS/JS nodig:
  - Bronmateriaal: 40 foto's uit `/home/jefferson/Downloads/Photos-3-001(2)` — persoonlijke foto's verzameld over meerdere jaren (2017 t/m 2026, mix van gewone camera- en Pixel-telefoonbestanden inclusief Motion Photo-`.MP.jpg`-varianten). Volgorde bepaald via de datum/tijd die in elke bestandsnaam zit (`YYYYMMDD_HHMMSS` resp. `PXL_YYYYMMDD_HHMMSS`), dus chronologisch. Zelfde verkleiningsaanpak als de huisje-foto's (max. 1600px, kwaliteit 80, EXIF-rotatie behouden), hernoemd naar `assets/omgeving-fotos/omgeving-1.jpg` t/m `omgeving-40.jpg` — resultaat ~11MB totaal.
  - Eigen "Gallery"-knop en carrousel, volledig los van de huisje-strip: beide carrousels scrollen onafhankelijk van elkaar (geverifieerd: klikken in de omgeving-carrousel laat de huisje-carrousel ongewijzigd), en de gedeelde `#photo-lightbox`/`#gallery-grid-overlay`-componenten worden bij elke "Gallery"-klik opnieuw gevuld met de juiste foto-set — bladeren in de lightbox blijft daardoor altijd binnen de set die geopend werd (omgeving-10 → Next → omgeving-11, geen doorloop naar huisje-foto's).
  - Geverifieerd via headless Chromium (CDP) op desktop (1280×900) en mobiel (390×844): juiste volgorde van secties op de pagina (huisje-carrousel → knoppengroep → omgeving-carrousel), 22 vs. 40 slides correct geladen, onafhankelijke carrousel-scroll, omgeving-gallery-grid toont 40 omgevingsfoto's met eigen titel, lightbox-navigatie blijft binnen de juiste set, hero blijft `paused:false`/zichtbaar tijdens grid- en lightbox-gebruik van deze nieuwe sectie, en regressie-check bevestigt dat de huisje-gallery nog steeds correct werkt na gebruik van de omgeving-gallery (het gedeelde grid/lightbox wisselt correct van foto-set).
- Autoplay-snelheid van beide carrousels (huisje én omgeving) verdubbeld: `AUTOPLAY_INTERVAL` in `hero.js` van 3500ms naar 7000ms. Pauze-bij-hover/interactie en de hervat-vertraging (4s) ongewijzigd. Geverifieerd via CDP: beide carrousels stappen nu na ~7s door i.p.v. ~3,5s.
- Alle foto's in `assets/huisje-fotos/` (22) en `assets/omgeving-fotos/` (40) bekeken en feitelijk beschreven t.b.v. tekstschrijven (kamers/ruimtes, opvallende kenmerken, sfeer voor het huisje; locaties/herkenbare elementen/sfeer voor de omgeving — overwegend Carvoeiro-dorp: witte huizen op okerkleurige kliffen, strand, zonsondergangen, rotsformaties/grotten, boulevard-restaurants "Levels"/"Don Carvoeiro"/"Bote"). Klant koos uiteindelijk voor korte, algemene tekst i.p.v. een uitgebreide, foto-specifieke beschrijving.
- Placeholder-tekst op `index.html` vervangen door definitieve (korte) tekst, twee plekken:
  - Intro boven de huisje-carrousel: kop "Uw plek in Monte Dourado" + korte alinea (Monte Dourado Resort, gedeeld zwembad, wandelafstand Carvoeiro, ontspannen uitvalsbasis).
  - Nieuwe intro-sectie toegevoegd boven de omgeving-carrousel (bestond nog niet, analoog aan de huisje-intro qua structuur/stijl): kop "Omgeving" + korte alinea (sfeervol kustdorpje, witgekalkte huizen, goudkleurige kliffen, boulevard, alles binnen enkele minuten).
  - Carrousel-toolbar-titels (klein label naast de "Gallery"-knop) hernoemd van "Foto's van het huisje"/"Foto's van de omgeving" naar "Appartement"/"Omgeving" — inclusief de bijbehorende `title`-parameter in `initPhotoStrip()`, zodat de Gallery-popup-titel hetzelfde label toont.
  - Geverifieerd via full-page screenshot op desktop (1280px breed): beide teksten en beide carrousel-titels tonen correct.
- **De kleine carrousel-labels ("Appartement"/"Omgeving") weer verwijderd** op verzoek — overbodig naast de grotere intro-teksten erboven. `<h3 class="gallery-strip-title">` weggehaald uit beide `.gallery-toolbar`-blokken in `index.html`; de nu ongebruikte `.gallery-strip-title`-CSS-regel is ook verwijderd. `.gallery-toolbar` stond op `justify-content: space-between` (voor twee elementen, titel + knop) en is omgezet naar `flex-end` zodat de "Gallery"-knop met één overgebleven kind netjes rechts uitgelijnd blijft staan, op dezelfde hoogte als waar de carrousel begint. De `title`-parameter in `initPhotoStrip()` (gebruikt voor de Gallery-popup-titel) is ongewijzigd gebleven. Geverifieerd via full-page screenshot op desktop: geen label meer tussen intro-tekst en carrousel, Gallery-knop netjes uitgelijnd op beide secties.
- Groene footer-tekst bijgewerkt, met bewust verschil tussen de pagina's: `index.html` toont nu "Uw rustpunt aan de kust van de Algarve." (was "Met zorg samengesteld voor de gasten van het huisje"); `in-de-buurt.html` blijft ongewijzigd met de oorspronkelijke tekst. Bevestigd via curl dat beide pagina's de juiste, verschillende tekst tonen.
- **Subtiele achtergrond-band ("swimlane") toegevoegd op `index.html`** rond de huisje-carrousel: nieuwe CSS-variabele `--band-tint: #EEF6F5` (zeer lichte mint/teal-tint, afgeleid van het bestaande `--teal`/`--teal-light`-palet, herbruikbaar voor toekomstige banden) en een nieuwe `.swimlane`-wrapper-div in `index.html` die exact de huisje-`gallery-section` (toolbar met Gallery-knop + carrousel) én de daaropvolgende `cta-section` (knoppengroep) omvat — begint direct boven de Gallery-knop, eindigt direct onder de knoppengroep. De intro-tekst erboven ("Uw plek in Monte Dourado") en de volledige "Omgeving"-sectie eronder blijven op de gewone crème-achtergrond, geen band daar. Geverifieerd via screenshot op desktop (1280px) en mobiel (390px): band dekt exact het beoogde gebied, geen harde randen, rustige overgang.
- Ruimte tussen het einde van de omgeving-carrousel en de groene footer op `index.html` vergroot: `#gallery-omgeving` heeft nu een specifieke `padding-bottom: 80px` (was 32px via de gedeelde `.gallery-section`-regel, dus ruim 2,5× zo groot), alleen op deze sectie van toepassing omdat die direct vóór de footer staat — de huisje-sectie (binnen de swimlane, gevolgd door de knoppengroep) is ongewijzigd. Geverifieerd via CDP-meting (80px gap) en screenshot op desktop.
- **Fotocarrousel (gedeelde component, beide fotostrips) gefixt op mobiel-portret**: er was een storend randje van de volgende foto zichtbaar op smalle schermen (`.carousel-slide` stond op `flex-basis: 82%` onder 640px, bedoeld als bewuste "peek" voor bredere mobiele/liggende schermen maar te breed voor portret). Nieuwe media query toegevoegd op `max-width: 480px` die `.carousel-slide` naar `flex-basis: 100%` zet — precies 1 foto tegelijk, geen gedeeltelijke volgende/vorige foto meer zichtbaar. Brede schermen (tablet, desktop, landscape-mobiel >480px breed) blijven ongewijzigd (2 of 3 naast elkaar, zoals al goed werkte). Pijl-navigatie en autoplay blijven gewoon werken, nu per foto i.p.v. per groep. Geverifieerd via CDP op 375×812, 390×844 en 428×926 (portret, telkens `visibleFraction: 1`, geen randje) en op 812×375 en 844×390 (landscape, ongewijzigd 2-up-gedrag met bewuste peek).

## Open punten

- Swipe-gedrag van de fotocarrousels (huisje én omgeving) handmatig testen op een echt mobiel toestel (kon niet headless geverifieerd worden, zie eerdere aantekening bij de huisje-strip)
- Definitieve introtekst voor de "Gallery"-overlays (nu placeholdertekst, voor beide fotostrips)
- **QR-code bij klant moet vervangen worden** zodra deze herstructurering live staat — de huidige QR wijst naar de root-URL, die nu de (nog lege) hoofdpagina toont in plaats van de gastengids
- Nieuw design-plan nodig vóór herbouw van de content-pagina (`in-de-buurt.html`) — het eerdere concept is afgekeurd als "te standaard/generiek", Fase 0 (design) moet opnieuw doorlopen worden
- Foto's/sfeer-input van klant nog ontvangen
- Instagram-accounts klant nog navragen
- GetYourGuide affiliate-account klant: onbekend
- Zest Car Rental affiliate-link: nog niet zelf getest door klant
- Alle externe links controleren — Zest Car Rental affiliate-link, eventuele GetYourGuide-link, en straks de Instagram-links zodra ontvangen. Checken of ze daadwerkelijk naar de juiste/werkende pagina's verwijzen.
- Klopt de naam/locatie van restaurant Avola? Niet online vindbaar.
- Is Manoels een apart restaurant naast Black Salt, of hoort het er bij / andere naam bedoeld?
- Bestaat Xamor nog als bar naast Levels, of is dit verouderd?
- Klopt 'Mais Perto' als supermarktnaam? Niet online vindbaar.

## Recente besluiten

- Hosting via GitHub Pages
- Stack blijft static HTML/CSS/JS
- Zest-link blijft exact zoals klant aanleverde
- Preview A (effen achtergrond) gekozen als basis, preview B verworpen
